import GeneralAdmissions from "@/app/ui/[city]/general-admission/page";
import NavLinks from "../nav-links";
const Page: React.FC = async () => {
  return <GeneralAdmissions />;
};

export default Page;
